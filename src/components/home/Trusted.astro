<section class="container mt-20 mx-auto">
  <div class="px-8 md:px-16">
    <p class="mb-4 text-center text-sm text-base-content/60">
      Akan Dipercayai oleh 30+ Perusahaan nasional dan Multinasional
    </p>
    <div
      class="mx-auto grid grid-cols-2 items-center gap-6 opacity-70 sm:grid-cols-3 md:grid-cols-6"
    >
      {
        Array.from({ length: 6 }).map((_, i) => (
          <div class="h-8 rounded bg-gray-200" />
        ))
      }
    </div>
  </div>

  <div class="relative rounded-b-3xl bg-primary py-12 text-white mt-12">
    <div class="mx-auto px-8 md:px-16">
      <div class="mb-10 grid grid-cols-1 gap-6 sm:grid-cols-3 md:grid-cols-3">
        <div>
          <h3 class="text-2xl font-semibold">
            Bagi Kandidat Tidak Terlewat oleh Pekerjaan Baru Lagi
          </h3>
        </div>
        <div class="border-l-4 pl-6">
          <p class="text-2xl font-bold">10+</p>
          <p class="opacity-80">Total Pekerjaan Hari Ini</p>
        </div>
        <div class="border-l-4 pl-6">
          <p class="text-2xl font-bold">40+</p>
          <p class="opacity-80">Total Pekerjaan</p>
        </div>
      </div>

      {/* job cards */}
      <div class="marquee-container">
        <div class="marquee-content">
          <div class="flex gap-3 md:gap-4">
            {
              Array.from({ length: 8 }).map((_, i) => (
                <div class="flex-shrink-0 flex items-center gap-3 p-3 md:p-4 w-60 md:w-72">
                  <div class="h-8 w-8 md:h-10 md:w-10 rounded-full bg-primary-content/30" />
                  <div>
                    <p class="mb-1 md:mb-2 text-xs opacity-80">
                      Your Company · 1h ago
                    </p>
                    <h4 class="text-xs md:text-sm font-semibold">
                      Software Engineer {i + 1}
                    </h4>
                  </div>
                </div>
              ))
            }
          </div>
          <div class="flex gap-3 md:gap-4">
            {
              Array.from({ length: 8 }).map((_, i) => (
                <div class="flex-shrink-0 flex items-center gap-3 p-3 md:p-4 w-60 md:w-72">
                  <div class="h-8 w-8 md:h-10 md:w-10 rounded-full bg-primary-content/30" />
                  <div>
                    <p class="mb-1 md:mb-2 text-xs opacity-80">
                      Your Company · 1h ago
                    </p>
                    <h4 class="text-xs md:text-sm font-semibold">
                      Software Engineer {i + 9}
                    </h4>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
